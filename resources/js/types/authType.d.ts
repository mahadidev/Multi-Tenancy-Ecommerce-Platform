export interface UserType {
    name: string;
    email: string;
    accessToken: string;
}
export interface UserProfileType {
    id: string;
    name: string;
    email: string;
    phone: string;
    address: string;
}
